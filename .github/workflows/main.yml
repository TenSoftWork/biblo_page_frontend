name: CI/CD for my-frontend

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      # 1) 체크아웃
      - name: Check out code
        uses: actions/checkout@v2

      # 2) Docker 로그인
      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      # 3) Docker 이미지 빌드 및 푸시
      - name: Build and push
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/my-frontend:latest .
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/my-frontend:latest
      
      - name: Trigger jenkins Deployment
        run: |
          curl -X POST "http://112.219.163.139:8080/job/biblo_homepage_FE/build?token=11255b2c8b4064d1325ba36522e5d01c6b" \
          -H "Jenkins-Crumb: 0853c94680e99969a43c5b256f0e689380e6f7277e03111bcb0e2d7b4a3eea47" \
          -u tsw_jenkins:11255b2c8b4064d1325ba36522e5d01c6b
