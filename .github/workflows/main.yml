name: CI/CD for my-frontend
on:
  push:
    branches: [ "main" ]
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      # 1) 체크아웃
      - name: Check out code
        uses: actions/checkout@v2
      
      # 2) Docker 로그인
      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
      
      # 3) Docker 이미지 빌드 및 푸시
      - name: Build and push
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/my-frontend:latest .
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/my-frontend:latest
      
      # 4) SSH를 통한 Jenkins 작업 트리거 (새 키 사용)
      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.JENKINS_SSH_PRIVATE_KEY }}
          known_hosts: ${{ secrets.JENKINS_KNOWN_HOSTS }}
          if_key_exists: replace
      
      - name: Trigger Jenkins Deployment via SSH
        run: |
          ssh -p 1398 kjm@112.219.163.139 'curl -X POST "http://localhost:8080/job/biblo_homepage_FE/build?token=11255b2c8b4064d1325ba36522e5d01c6b"'
